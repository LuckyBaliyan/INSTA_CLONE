==> Auth
   ==>Register
   ==>Login
   ==>Logout (token blacklisting)
   ==>Otp based registration

==>POST
   ==>create
   ==>can see the feed
   ==>like POST
   ==>save POST

==>USERS
   ==>followers
   ==>following


USER{
   name:String
   email:String 
   pass:String
   bio:String
   followers:array
   profile_img:String 
}

use a cloud url provider we just uplaod img and generate url form there and rest
will be handled by img kit and we can store that url in db